default:
  artifacts:
    paths:
      - public

lint:
  image: node:alpine
  cache:
    paths:
      - node_modules
  script:
    - npm install csslint htmlhint -g --no-audit
    - csslint style.css
    - htmlhint index.html
  after_script:
    - mkdir public
    - mv *.* public

pages:
  stage: deploy
  variables:
    GIT_STRATEGY: none
  image: alpine
  script:
    - gzip -k public/*
  only:
    - master
